# --- Email (SMTP / Nodemailer) ---
# Renseignez les valeurs réelles dans .env (non versionné)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# --- Base de données PostgreSQL ---
# Format : postgresql://utilisateur:motdepasse@hote:port/nom_base
DATABASE_URL=
