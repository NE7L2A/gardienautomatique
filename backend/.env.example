# --- Email (SMTP / Nodemailer) ---
# Renseignez les valeurs réelles dans .env (non versionné)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# --- Base de données PostgreSQL (Render) ---
# Chaîne de connexion (URL interne dpg-…-a sans domaine en production)
DATABASE_URL=
